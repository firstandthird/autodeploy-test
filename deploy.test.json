{
  "name": "testapptest",
  "rootDomain": "master",
  "https": true,
  "dockerargs": {
    "e": {
      "PORT": 5000,
      "AD_TEST": 3,
      "NODE_ENV": "{{env}}",
      "GH_USERNAME": "{{vars.GH_USERNAME}}"
    },
    "p": 5000
  },
  "scale": 3
}
