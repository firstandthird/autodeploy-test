{
  "name": 'testapp',
  "dockerargs": {
    "e": {
      "PORT": 5000,
      "AD_TEST": 1,
      "NODE_ENV": "{{env}}",
      "GH_USERNAME": "{{vars.GH_USERNAME}}"
    },
    "p": 5000
  },
  "scale": {
    "production": 3,
    "dev": 2
  }
}
