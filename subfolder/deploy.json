{
  "folder": "subfolder",
  "dockerargs": {
    "e": {
      "PORT": 5000,
      "AD_TEST": 2,
      "NODE_ENV": "{{env}}",
      "GH_USERNAME": "{{vars.GH_USERNAME}}",
      "VIRTUAL_HOST": "{{branch}}.{{repo}}.dev2.1n3.co",
      "SUBFOLDER": 1
    },
    "p": 5000
  },
  "scale": 2
}
